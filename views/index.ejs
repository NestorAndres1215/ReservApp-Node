<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sistema de Reservas | Inicio</title>
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    /* Estilos generales */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    
    body {
      background-color: #ffffff;
      color: #1a1a1a;
      line-height: 1.6;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    
    /* Contenedor principal */
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 2rem;
      text-align: center;
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }
    
    /* Estilos del encabezado */
    h1 {
      font-size: 2.8rem;
      margin-bottom: 2.5rem;
      color: #d10a11; /* Rojo intenso */
      animation: fadeIn 1s ease-in;
      position: relative;
      display: inline-block;
    }
    
    h1::after {
      content: '';
      position: absolute;
      bottom: -10px;
      left: 50%;
      transform: translateX(-50%);
      width: 80px;
      height: 4px;
      background: #d10a11;
      border-radius: 2px;
    }
    
    /* Estilos de los enlaces */
    .auth-links {
      margin-top: 3rem;
      display: flex;
      gap: 1.5rem;
      flex-wrap: wrap;
      justify-content: center;
    }
    
    .auth-links a {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 1rem 2rem;
      background-color: #d10a11;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 600;
      transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
      box-shadow: 0 4px 6px rgba(209, 10, 17, 0.25);
      border: none;
      font-size: 1.1rem;
    }
    
    .auth-links a:hover {
      background-color: #b0080e;
      transform: translateY(-5px) scale(1.03);
      box-shadow: 0 10px 15px rgba(209, 10, 17, 0.3);
    }
    
    .auth-links a:active {
      transform: translateY(2px) scale(0.98);
    }
    
    .auth-links a:nth-child(2) {
      background-color: #ffffff;
      color: #d10a11;
      border: 2px solid #d10a11;
      box-shadow: 0 4px 6px rgba(209, 10, 17, 0.1);
    }
    
    .auth-links a:nth-child(2):hover {
      background-color: #f8f8f8;
      box-shadow: 0 10px 15px rgba(209, 10, 17, 0.2);
    }
    
    /* Separador */
    .separator {
      color: #d10a11;
      font-weight: bold;
      align-self: center;
      display: flex;
      align-items: center;
    }
    
    /* Animaciones */
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    
    /* Pie de página */
    footer {
      text-align: center;
      padding: 1.5rem;
      background-color: #d10a11;
      color: #ffffff;
      margin-top: auto;
      font-size: 0.9rem;
    }
    
    .footer-content {
      display: flex;
      justify-content: center;
      gap: 1rem;
    }
    
    .footer-content a {
      color: #ffffff;
      transition: color 0.3s;
    }
    
    .footer-content a:hover {
      color: #ffccd5;
    }
    
    /* Efecto decorativo */
    .decoration {
      position: absolute;
      top: 0;
      right: 0;
      width: 200px;
      height: 200px;
      background: radial-gradient(circle, rgba(209,10,17,0.1) 0%, rgba(255,255,255,0) 70%);
      z-index: -1;
    }
    
    /* Responsive */
    @media (max-width: 768px) {
      h1 {
        font-size: 2.2rem;
      }
      
      .auth-links {
        flex-direction: column;
        align-items: center;
        gap: 1.2rem;
      }
      
      .auth-links a {
        width: 100%;
        justify-content: center;
      }
      
      .separator {
        display: none;
      }
      
      .footer-content {
        flex-direction: column;
        gap: 0.5rem;
      }
      
      .decoration {
        width: 150px;
        height: 150px;
      }
    }
  </style>
</head>
<body>
  <div class="decoration"></div>
  <div class="container">
    <h1>Bienvenido al Sistema de Reservas <i class="fas fa-utensils"></i></h1>
    <div class="auth-links">
      <a href="/login">
        <i class="fas fa-sign-in-alt"></i> Iniciar Sesión
      </a>
      <span class="separator">|</span>
      <a href="/register">
        <i class="fas fa-user-plus"></i> Registrarse
      </a>
    </div>
  </div>
  
  <footer>
    <div class="footer-content">
      <span>&copy; <%= new Date().getFullYear() %> Sistema de Reservas</span>
      <a href="#"><i class="fas fa-shield-alt"></i> Privacidad</a>
      <a href="#"><i class="fas fa-file-contract"></i> Términos</a>
      <a href="#"><i class="fas fa-envelope"></i> Contacto</a>
    </div>
  </footer>
</body>
</html>